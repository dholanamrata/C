#include<stdio.h>
#include<conio.h>
main()
{	int ;
	clrscr();
	printf("enter the age");
	scanf("%d",&age);
	if(age>=18)
	{	printf("eligible for vote",age);

	}
	else
	{	printf(" not eligible for vote",age);

	}
	getch();
}
